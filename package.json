{
  "name": "sunflowerland-wiki",
  "version": "0.1.0",
  "private": true,
  "description": "Fan wiki for Sunflower Land, generated from the official game repository",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "sync": "tsx scripts/sync-reference.ts",
    "extract": "tsx scripts/extract/index.ts",
    "generate": "tsx scripts/generate/index.ts",
    "check-updates": "tsx scripts/check-updates.ts",
    "pipeline": "npm run sync && npm run extract && npm run generate",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitepress": "^1.6.3"
  }
}
